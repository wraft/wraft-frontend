{
  "typescript": true,
  "replaceAttrValues": {
    "#2C3641": "{props.color || `#2C3641`}",
    "#FFFFFF": "{props.color || `#FFFFFF`}",
    "white": "{props.color || `#FFFFFF`}",
    "#111111": "{props.color || `#000000`}",
    "black": "{props.color || `#000000`}",
    "#000": "{props.color || `#000000`}"
  },
  "removeViewBox": false,

  "svgProps": {
    "width": "{ props.width || props.fontSize || 24}",
    "height": "{props.height || props.fontSize || 24}"
  }
}
