import React from 'react';
import { Box, Text } from 'rebass';

// Menu Template - ?

export const Menu = (props: any) => (
  <Box pt={1} sx={{ p: 1, width: 'auto' }} {...props}>
    <Text fontSize={0}>Menu</Text>
  </Box>
);


// Text Editor - Remirror

export const TextEditor = (props: any) => (
  <Box pt={1} sx={{ p: 1, width: 'auto' }} {...props}>
    <Text fontSize={0}>TextEditor</Text>
  </Box>
);
