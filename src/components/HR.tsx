import { Box } from 'theme-ui';

const HR = () => {
  return (
    <Box
      sx={{
        width: '100%',
        height: '1px',
        my: '200px',
        bg: 'neutral.0',
      }}
    />
  );
};

export default HR;
